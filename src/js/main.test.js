/* import overlays from './main.js';

describe('overlays', () => {
  test('overlays(string)', () => {
    //expect(overlays('cool')).toMatchSnapshot();
    expect(overlays({
      start: 'pause',
      end: 'play'
    })).toBeDefined();
  });
}); */
